/**
 * /project/[id]/wiring — Connection graph and pin list.
 */

import { WiringPanel } from '@/components/workspace/panels/WiringPanel';

export default function WiringPage() {
  return <WiringPanel />;
}
