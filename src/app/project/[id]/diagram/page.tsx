/**
 * /project/[id]/diagram — Diagram canvas and simulator export.
 */

import { DiagramPanel } from '@/components/workspace/panels/DiagramPanel';

export default function DiagramPage() {
  return <DiagramPanel />;
}
