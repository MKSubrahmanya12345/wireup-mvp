/**
 * /project/[id]/firmware — Generated firmware and libraries.
 */

import { FirmwarePanel } from '@/components/workspace/panels/FirmwarePanel';

export default function FirmwarePage() {
  return <FirmwarePanel />;
}
