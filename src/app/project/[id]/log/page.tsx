/**
 * /project/[id]/log — The raw, per-event run log (advanced / for the curious).
 */

import { RunLogPanel } from '@/components/workspace/panels/RunLogPanel';

export default function LogPage() {
  return <RunLogPanel />;
}
