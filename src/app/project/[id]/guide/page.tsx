/**
 * /project/[id]/guide — The step-by-step build guide.
 */

import { GuidePanel } from '@/components/workspace/panels/GuidePanel';

export default function GuidePage() {
  return <GuidePanel />;
}
