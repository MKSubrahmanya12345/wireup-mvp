/**
 * /project/[id]/hands — Hands & legs: the physical tasks only a human can do.
 */

import { HandsLegsPanel } from '@/components/workspace/panels/HandsLegsPanel';

export default function HandsPage() {
  return <HandsLegsPanel />;
}
