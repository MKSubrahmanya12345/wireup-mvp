/**
 * /project/[id]/quality — Validation verdict and fixes.
 */

import { QualityPanel } from '@/components/workspace/panels/QualityPanel';

export default function QualityPage() {
  return <QualityPanel />;
}
